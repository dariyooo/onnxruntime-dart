/// What the operator library is called.
///
/// Imports nothing, so both halves of the seam can name it.
library;

/// The library's file name without prefix or extension.
const extensionsLibraryStem = 'ortextensions';
